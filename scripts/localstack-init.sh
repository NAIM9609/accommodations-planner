#!/usr/bin/env bash
# DEPRECATED — no longer used.
# DynamoDB table creation and all other resource deployment is now handled by
# the 'deploy' docker-compose service via scripts/deploy-local.sh.
echo "This script is deprecated. Run 'docker compose up' to start the full local stack."
