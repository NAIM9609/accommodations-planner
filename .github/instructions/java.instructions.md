---
applyTo: "**/*.java"
---

# Java Instructions

- Follow existing project conventions and formatting.
- Prefer clear, maintainable code changes that preserve behavior.
- Keep imports minimal and avoid unnecessary dependencies.
- Use standard Java naming conventions (camelCase for methods/fields, PascalCase for classes).
- Prefer explicit types over `var` for public APIs.